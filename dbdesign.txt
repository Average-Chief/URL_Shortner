urls
- id
- original_url
- short_code
- access_count
- created_at
- updated_at


url_shortener/
│
├── app.py            # App entry + route registration
├── routes.py         # All API endpoints
├── models.py         # URL model (DB schema)
├── storage.py        # DB / persistence logic
├── services.py       # Core business logic
├── errors.py         # Error definitions & helpers
├── utils.py          # Shortcode generation, URL validation
│
├── .env
├── README.md
└── requirements.txt

services.py
│
├── create_short_url(url)
├── get_short_url(short_code)
├── update_short_url(short_code, new_url)
├── delete_short_url(short_code)
├── redirect_short_url(short_code)
├── get_url_stats(short_code)
